generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model User {
  name        String    @id   @default(uuid())
  username    String
  password    String
}